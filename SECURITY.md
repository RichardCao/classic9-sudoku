# 安全策略

本项目是本地 TypeScript 数独工具库，不应执行不可信代码，也不应访问网络。

## 支持版本

当前支持 `0.1.x`。

## 报告问题

如果发现安全问题，请在公开发布仓库后通过维护者指定渠道报告。

报告时请包含：

1. 影响范围。
2. 复现步骤。
3. 输入样例。
4. 预期行为和实际行为。

## 安全边界

当前重点安全边界：

1. CLI 只读取用户显式传入的 JSON 或题面。
2. 不执行候选池中的动态代码。
3. 不从网络加载题库或规则。
4. 不依赖外部应用工程路径。
5. 发布包不包含临时生成文件。
